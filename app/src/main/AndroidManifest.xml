<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />

    <application
        android:allowBackup="true"
        android:usesCleartextTraffic="true"
        android:icon="@mipmap/ic_launcher"
        android:label="NetScanner"
        android:theme="@style/AppTheme">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ScanActivity" />
        <activity android:name=".MultiPingActivity" />
        <activity android:name=".PortScanActivity" />
        <activity android:name=".HistoryActivity" />
        <activity android:name=".tools.ToolsActivity" />
        <activity android:name=".LocalPortsActivity" />
        <activity android:name=".tools.NetDiagActivity" />
        <activity android:name=".tools.ToolRunnerActivity" />
        <activity android:name=".tools.ConnectionsActivity" />
        <activity android:name=".tools.WifiAnalyzerActivity" />
        <activity android:name=".tools.SshActivity" />
        <activity android:name=".tools.SnifferActivity" />
        <activity android:name=".tools.PingMonitorActivity" />
        <activity android:name=".LogsActivity" />
        <activity android:name=".SpeedHistoryActivity" />
        <activity android:name=".UsageActivity" />
        <activity android:name=".SignalActivity" />
        <activity android:name=".CellMonitorActivity" />
        <activity android:name=".WolActivity" />        <activity android:name=".HealthActivity" />

        <service android:name=".tools.SnifferVpnService"
            android:permission="android.permission.BIND_VPN_SERVICE"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <intent-filter>
                <action android:name="android.net.VpnService" />
            </intent-filter>
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="DNS sniffer - monitors DNS queries via local VPN" />
        </service>

        <service android:name=".CellMonitorService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Live cell signal monitoring" />
        </service>

        <activity android:name=".AutoSpeedActivity" />
        <activity android:name=".DnsTesterActivity" />

        <service android:name=".AutoSpeedService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Periodic scheduled speed tests at user-chosen intervals" />
        </service>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>
